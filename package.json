{
  "name": "emi-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --recursive --filter ./packages/** build",
    "dev": "pnpm --recursive --parallel --filter ./packages/** dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.10",
    "typescript": "^5.3.3"
  }
}
