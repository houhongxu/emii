{
  "scripts": {
    "build": "pnpm --recursive --filter=./packages/** build",
    "dev": "pnpm --recursive --parallel --filter=./packages/** dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
